# Dockerfile.agent
FROM python:3.11-slim

RUN pip install --no-cache-dir \
    httpx \
    "livekit-agents[silero,openai]" \
    "faster-whisper" \
    "edge-tts"

COPY agent.py /app/
WORKDIR /app

CMD ["python", "agent.py", "start"]
